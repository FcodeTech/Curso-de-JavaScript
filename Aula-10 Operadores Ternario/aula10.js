// Declaração das variáveis
let num1 = 10
let num2 = 5

// Teste lógico Par ou Impar
res = (!(num1 % 2) ? "Par" : "Impar")
console.log(res)

// SINTÁX
// Teste lógico ? se verdadeiro : se falso
//0 = False
//1 = True

// Tem que negar "!" a condição Ex: (!(num % 2) ? "Par" : "Impar")


res2 = (num1 > num2 ? "Verdadeiro" : "Falso")
console.log(res2)